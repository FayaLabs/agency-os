# Leave these EMPTY to run Agency OS in mock mode (in-memory + localStorage).
# Set them to point at a Supabase project to switch auth/org/providers to live data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
